# 场景 44 自然胜利与铃铛目标证据（2026-07-18）

## 结论

场景 `0x2C` 已在 immutable base ROM 上自然完成。它是**两阶段**目标：先击倒卡卡西，再取得北部宝箱表示的铃铛。仅把卡卡西的 character ID 清零不会结算；游戏内“相关条件”页明确显示“拿到卡卡西老师的铃铛”。

正确触发点是宝箱上方 **`(4,3)`**：角色朝下结束行动后立即进入鸣人与卡卡西的剧情，随后结果位写为 1 并出现胜利页。石块 `(3,9)` 不可通行；宝箱左侧 `(3,4)` 与直接占据箱体 `(4,4)` 都没有触发结算，因此不能把“靠近或占格”泛化为胜利条件。

稳定恢复点为 `artifacts/runtime-checkpoints/scenario-44-postbattle-world-map.ss9`，SHA-256 是 `b452a77ef623d36423ec19eb4b5635d58d186ec041fa7891b356889d367875cd`。标准零输入复放 run `66782e8c7b989f8d136423e60262ceb6` 成功，task 2 resume 为 `0x08067D02`，RGB hash 为 `80f987fd20333739f3710ba0b0f59ce83f2ae9ab66fdfa3c34314c49cea6cb69`。

## 自然奖励

结果页给三名队员各 **125 EXP**。稳定模板记录为：

- Naruto：LV3、EXP 125、训练点 2；
- Sasuke：LV2、EXP 45、训练点 1；
- Sakura：LV1、EXP 125、训练点 0。

这证明同一场战斗的相同 EXP 奖励会因各角色升级阈值不同而形成不同 level/余量，不能只用 Naruto 模板代表全队成长。

## 战斗与资源边界

- Kakashi 初始 100 HP，在记录仍保存 6 HP 时进入击倒状态；击倒后战斗仍继续，证明敌方清零只是第一阶段。
- 正式零输入复放峰值 52.04296875 MiB；批量导航最高 **107.890625 MiB**，低于 140 MiB 守卫上限，未发生内存异常。
- 批量 Lua 导航的退出崩溃只发生在预定文件写完后的清理阶段；正式 publication 由标准零输入 runner 独立验证。
- 本检查点只证明场景 44 的自然胜利、奖励和返回世界地图，不覆盖后续任务。
