# 场景 46 前置剧情恢复点（2026-07-19）

## 当前结论

场景 45 世界地图后的下一主线已自然推进到任务委托人首次发言。稳定恢复点为
`artifacts/runtime-checkpoints/scenario-46-client-dialogue.ss9`，SHA-256 是
`5d50c5fc814471dacec0cb2f132e6fe0a15172373cb634aa225fde10bcf24ca6`。
标准 80 帧零输入复放 run `ac4fa41e596b7f88168348ef807353f9` 成功，峰值 owned-tree RSS
52.85546875 MiB，`pgid_clean=true`。本检查点仍是任务前置对白，不证明场景 46 已进入战斗。

## 自然路线

1. 从 `scenario-45-postbattle-world-map.ss9` 选择默认移动目的地，进入一乐拉面剧情。
2. 完成鸣人与伊鲁卡/手打对白并取得剧情奖励，返回木叶地图。
3. 再选择火影办公室，完成第七班、卡卡西与三代火影的任务分级说明和 D 级任务简报。
4. 当前画面已出现新委托人并进入其第二句对白；后续直接从正式 checkpoint 继续，不重放此前
   68 个单 A 边界。

## 超时与恢复

- 从 `dialogue-a67-zero.ss9` 用 300 帧单输入 runner 发送 A，两次都在约 20–23 秒触发
  `idle-timeout`；两次峰值 RSS 约 52.2 MiB，均由进程组守卫清理，无新 macOS mGBA
  崩溃报告、无 crash latch、无进程残留。
- 同一输入改为 80 帧捕获后成功，画面进入委托人下一句对白；再从该短帧状态做 80 帧零输入
  复放也成功。因此输入和游戏状态有效，失败边界是该场景在长捕获期间超过 15 秒没有 runner
  可观察进度，不应误报为 mGBA 崩溃。
- 后续在这段对白使用 80 帧检查点，并在跨场景后重新校准；不得简单取消 idle timeout。

## 下一步

从正式 checkpoint 继续委托人对白，遇到任务标题、编队或可操作地图时固化下一检查点；
减少截图展开，只在状态类型变化时检查画面，以控制 Codex 桌面渲染内存和上下文体积。

## 编队检查点续进（16:21–16:32）

- 从 `scenario-46-client-dialogue.ss9` 继续 68 个单 A 边界，完整推进达兹纳委托说明、
  第七班争执和出发剧情，最终进入“队伍・装备”编队界面。
- 该段统一使用 80 帧单输入检查点；`build/scenario-46-resume-20260719` 共 69 份含正式
  零输入复验的 audit，全部 `success=true`、`pgid_clean=true`，最高 owned-tree RSS
  52.4140625 MiB。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-lineup.ss9`，SHA-256
  `e91f1a72b59d6ee00e180818d9aa51679525c6fff13b9147f893fc97667c10a3`；80 帧零输入
  run `be4c7240ba89e8d936f997d55b0087b8` 成功，峰值 RSS 52.08203125 MiB。
- macOS mGBA crash report 仍为 25，未生成 crash latch，检查点结束后无 mGBA 进程残留。
- 下一轮直接从编队检查点选择“开始任务”并建立场景 46 可操作地图快照，不再重放本页记录的
  前置对白。

## 战斗起点检查点续进（16:36–16:45）

- 从编队页按 B 返回任务报告，依次选择“开始任务”并确认“是”，自然进入桥面战斗；这条路线
  没有使用内存写入、诊断 ROM 或自动输入脚本。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-battle-start.ss9`，SHA-256
  `eb5aa8b7887213a033b7b0e8886db2973fa76c7f360a373335aec7b719a8e3dd`。它由转场稳定状态再做
  80 帧零输入复放产生，run `8412352950a045e015b56636b31d6a0f` 成功，峰值 owned-tree RSS
  52.11328125 MiB，`pgid_clean=true`。
- 画面持续显示桥面上的三名角色和部署选择标记。离线 savestate 检查确认 task 2 resume 为
  `0x08073616`、battle ID 为 9、地图运行时尺寸为 `64×64 / 16×32`、结果 byte 仍为 0；
  后续单 A 实验确认这里仍是逐单位部署确认，不是玩家回合，因此不得把该画面冒充玩家控制。
- 后续直接从该检查点确认玩家行动菜单和本关胜负条件；每次只提交一个显式输入，并保留零输入
  对照，不再经过委托对白、编队和任务报告菜单。

## 玩家回合检查点续进（16:47–16:54）

- battle 9 的静态 formation variant 0 是 3 名玩家单位加 5 只猫。运行时从部署起点依次确认
  鸣人、佐助、小樱和五个敌方单位后出现“开始”，与该 8 条 active formation 记录一致。
- 战斗开始后的四个单 A 边界完成佐助与小樱的战前对白，随后鸣人画面出现“SELECT 详细”；
  再输入一个 A 后稳定打开鸣人的六项行动菜单，明确证明玩家控制已经接管。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-player-action-menu.ss9`，SHA-256
  `859ee3b54faa413d8e3f7a2cbbf428bb02b48a8174ba6337e0021d5cb006a8e1`。80 帧零输入复放
  run `cf2a64cbb1ab443a319dee0db5132427` 成功，峰值 owned-tree RSS 52.13671875 MiB，
  `pgid_clean=true`；task 2 resume 为 `0x08067D02`，battle ID 9、地图尺寸和未决结果 byte
  均保持不变。
- 该检查点证明可恢复的玩家行动菜单，但尚未证明 MOVEDONE、敌方阶段、胜利、EXP、升级或
  postbattle。后续应从这里选择移动或攻击，先定位本关胜负条件和最短自然通关路线。

## 首个自然行动与胜负条件（17:00–17:18）

- battle 9 variant 0 的胜利描述符位于 ROM file `0x594E84`：胜利组第一条为 type 1、
  affiliation 1，失败组为 type 1、affiliation 0。结合 `0x080777FC` 的已确认类型 1 扫描
  语义，本关要求敌方阵营无有效存活单位，即自然击倒五只猫；不是地点或回合数目标。
- 鸣人从行动菜单进入移动网格。游标可越过实际范围：`(11,20)` 被“请在移动范围内选择”
  拒绝，`(11,17)` 的地形是不可通行“河道”；正确桥列在右侧一格。最终有效路线为
  `(11,15) -> (12,15) -> (12,17)`，随后选择“行动结束”、面向下方并确认默认不防御。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-after-naruto-movedone.ss9`，
  SHA-256 `88faaff86d7d58255a502cd5dbe3c65c15e1614621a271043ef1fd12b9210d50`。
  80 帧零输入复放 run `d3c15842e53bff5a6a32de7913f753bf` 成功，峰值 owned-tree RSS
  52.390625 MiB，`pgid_clean=true`；画面已切换到小樱，鸣人坐标为 `(12,17)`，
  `0x0200A880..83 = 01 00 01 00`，battle result 仍为 0。
- 该检查点证明一次自然移动完成及下一单位接管，不证明整回合、敌方阶段或胜利。后续直接从
  小樱选择状态继续，不重做鸣人移动和两条无效河道候选。

## 第一回合完成与第二回合恢复点（17:18–17:33）

- 小樱和佐助均使用五项完整菜单的末项原地结束行动；完整菜单从首项按 Up 可直接回绕到
  “行动结束”。两人都完成朝向和默认不防御后，画面明确进入“回合 1／对手的行动”。
- 敌方五个单位的自动移动跨越四个 300 帧零输入窗口，随后出现鸣人/佐助两句回合对白并自然
  返回第二回合玩家选择。所有窗口均由同一 heavy guard 串行运行，没有自动补键或并发 mGBA。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-turn2-player.ss9`，SHA-256
  `1ffd04c267c457d8b27cdf5c38405060197a4f2871f1977cc61046ae6d2fb9e9`。300 帧零输入
  run `9413304e07e4ebd659b674e74cdd5ce1` 成功，峰值 owned-tree RSS 52.04296875 MiB，
  `pgid_clean=true`。
- 第二回合 unit records 显示我方 HP 为 109/89/80，五只猫的当前 HP 均为 1；formation
  record 中的 `0x001E` 因而不是当前 HP，不得解释为“猫有 30 HP”。敌方坐标已变为
  `(1,9)`、`(2,4)`、`(7,5)`、`(10,3)`、`(7,26)`，鸣人位于 `(12,17)`，小樱和佐助
  仍为 `(13,15)`、`(9,15)`。
- 该检查点证明完整玩家阶段、敌方自动阶段和第二回合接管；结果 byte 仍为 0。后续从这里沿桥
  追击南侧 `(7,26)` 敌人，每只敌人只需一次有效伤害即可进入击倒状态。

## 第二回合推进与纠错（17:37–17:55）

- 鸣人沿桥从 `(12,17)` 前进到 `(12,20)`；小樱从 `(13,15)` 移动到 `(12,17)`。
  佐助首次尝试从 `(9,15)` 移动到 `(12,16)` 时，游戏明确显示“请在移动范围内选择”；
  后续按键及 `enemy2-settle1..3` 因建立在该错误状态上全部不采纳，不作为回合证据。
- 从拒绝提示恢复后，将佐助目标修正为 `(12,15)` 并成功返回行动菜单；完成行动结束、朝向和
  默认不防御后，画面明确进入“回合 2／对手的行动”。这条纠正链与旧未采纳候选分开保存。
- 有效敌方阶段跨五个 300 帧零输入窗口，最终自然返回第三回合玩家控制。正式恢复点为
  `artifacts/runtime-checkpoints/scenario-46-turn3-player.ss9`，SHA-256
  `c7cc656c30ff851821c1b12ec6ff8538b1ae1b5384773b4bb4e78d32462be609`；最后一段 run
  `4935dacd9367ef54fddc147ad55c233c` 成功，峰值 owned-tree RSS 52.30078125 MiB，
  `pgid_clean=true`。
- 第三回合我方仍为 109/89/80 HP，坐标为鸣人 `(12,20)`、佐助 `(12,15)`、小樱
  `(12,17)`。五只猫仍为 1 HP，坐标变为 `(1,5)`、`(3,3)`、`(4,4)`、`(7,4)`、
  `(5,28)`，result 仍为 0；说明本轮只有移动，没有攻击命中。
- 后续从第三回合快照继续追击已接近地图南界的 `(5,28)` 敌人；恢复时不得使用错误的
  `(12,16)` 佐助候选或未采纳 `enemy2-settle*` 链。

## 南区追击恢复点（18:00–18:21）

- 第三回合鸣人从 `(12,20)` 绕屋顶移动到 `(9,20)`；小樱不能从 `(12,17)` 横穿左侧
  河道，纠正为沿桥到 `(12,20)`；佐助跟进到已腾空的 `(12,17)`。敌方阶段三个 300 帧
  零输入窗口后进入第四回合，南侧猫保持 `(5,28)`。
- 第四回合鸣人横向移动时，`(4,20)` 与 `(5,20)` 均被地图障碍拒绝，实际最左可停靠点是
  `(6,20)`；小樱跟进到 `(9,20)`，佐助到 `(12,20)`。下一敌方阶段后，南侧猫移动到
  `(9,28)`，其余四只仍聚集北区，我方保持无伤。
- 第五回合鸣人向南探测时，`(6,25)` 与 `(6,24)` 均被移动范围拒绝，实际可停靠边界是
  `(6,23)`。正式恢复点为
  `artifacts/runtime-checkpoints/scenario-46-south-approach-action-menu.ss9`，SHA-256
  `678730ffcaa05b3f96418f59722d7a840de43c757e6462c00fc2ef97ec9b13dd`；80 帧零输入
  run `5e3a1f37cd1fa26272d9fe45ef47a181` 成功，峰值 owned-tree RSS 52.25390625 MiB，
  `pgid_clean=true`。
- 该恢复点位于鸣人移动后的行动菜单，移动尚未通过“行动结束”最终提交；恢复后应直接选择
  行动结束，不能再次选择移动。南侧猫最近坐标是 `(9,28)`、HP 1，当前距离 8；后续一至两轮
  内继续接近并测试忍具/术攻击范围。

## 南侧敌人交战规则与第十六回合恢复点（18:21–19:08）

- 完整行动菜单的顺序经放大画面确认是“移动、术·忍具、直接攻击、休息、行动结束”；移动
  提交后菜单只剩“术·忍具、行动结束”，因此本作不能在同一回合移动后再用直接攻击。追击时
  必须先在相邻格结束，等下一玩家回合原地攻击，或改用允许的远程术/忍具。
- 第十二回合鸣人与南侧猫分别位于 `(6,28)`、`(5,28)`。从完整菜单选择第三项直接攻击后，
  目标预览显示猫 HP 1、预计伤害 12；自然结算后猫仍为 HP 1 且 active，符合场景 45 已记录的
  高命中率近战 RNG 未命中样本。该结果不得标成击杀；零输入状态 SHA-256 为
  `a3cea565d2489ad2aba8b0a2bb86bc5b70db979ea50f2b09dd94a767852b75c2`，run
  `925d748e4bd136105310746277ffdc08`。
- 全局菜单只能从无单位菜单覆盖的地图状态按 Start 打开；单位菜单仍打开时 Start 会被吞掉。
  正确的全局结束链是先按 B 退回地图，再按 Start，直接选择首个可选项“回合结束”，并把
  “可以吗？”光标从“否”切到“是”。该流程替代逐个结束未行动单位的重复操作。
- 南侧猫并非固定三点巡逻，而会持续远离邻接玩家：运行时先后观测 `(5,28)`、`(5,24)`、
  `(9,24)`、`(9,20)`、`(12,19)`。单人贴身追逐会形成移动后不能攻击、敌方阶段继续逃跑的
  循环；后续应让三人整体北推，在北边界与其余四猫汇合处围剿，并优先测试远程术/忍具。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-turn16-pursuit.ss9`，SHA-256
  `69c9b8598c28e4734f0500f2c4fb0d7dcffe5a7986b186f697c8aa0b0a598570`。零输入 run
  `f02a91c7bacdbbe254cb575def6c4b70` 成功，峰值 owned-tree RSS 52.2578125 MiB，
  `pgid_clean=true`；我方 HP 仍为 109/89/80，五只猫仍均为 HP 1，battle result 仍为 0。
- 本段结束时 macOS mGBA crash report 仍为 25，无新增崩溃报告、无 mGBA 进程残留；系统可用
  内存为 59%。后续直接从第十六回合恢复点北推，不重放第 6–15 回合的南区追逐。

## 长脉冲北推与跨河恢复点（19:18–19:42）

- 单次方向输入从 frame 5 持续到 frame 29，可在开放地形中把移动游标稳定推进三格，例如佐助
  `(5,23) -> (5,20)`、鸣人 `(6,25) -> (6,22)`。它仍是一个受审计输入，但把逐格三次启动
  mGBA 合并为一次；每次提交前仍离线读取 `0x02026A78..79` 并检查画面。
- 河水游标位置不等于可达目的地。佐助 `(5,20) -> (5,17)` 和小樱 `(10,19) -> (10,16)`
  的直上候选都穿过河水，未提交；有效桥列分别是左桥 `x=2` 和右桥 `x=12`。佐助先到
  `(2,20)` 再沿桥到 `(2,17)/(2,14)`；小樱以 `Right,Right,Up` 到右桥 `(12,18)`。
- 鸣人左侧受建筑墙阻挡，`(5,22)` 不能继续向左；从同一移动网格改向右后有效到达 `(8,22)`。
  所有水面、穿墙和错误单位顺序候选均从各自行动菜单或移动网格基线重做，未串入有效链。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-turn20-crossriver.ss9`，SHA-256
  `f5d65bdcf4adaa382e7fa1da634d240d278411e79c35c1415744f2f39bb2d532`。零输入 run
  `b5fedee78b91438886424323a32c2c61` 成功，峰值 owned-tree RSS 52.1796875 MiB，
  `pgid_clean=true`。
- 第二十回合我方坐标为鸣人 `(8,22)`、佐助 `(2,14)`、小樱 `(12,18)`，HP 仍为
  109/89/80。南侧猫已北逃到 `(14,5)`，其余四猫位于 `(10,3)/(4,3)/(2,3)/(11,3)`；
  五猫仍均为 HP 1，battle result 仍为 0。后续直接从跨河点继续北推并在 `y<=5` 围剿。
- 本段结束时 mGBA crash report 仍为 25，无 mGBA 进程残留，系统可用内存为 63%。

## 北侧墙体缺口与第二十三回合恢复点（20:37–20:52）

- 第二十至二十二回合继续整体北推：鸣人经右桥到 `(12,19)`；小樱沿北侧墙体从
  `(12,14)` 向左到 `(9,14)`；佐助从 `(8,14)` 直接向北到 `(8,11)`。运行时验证
  `(9,13)` 不可通行而 `(8,13..11)` 可通行，因此北侧墙体的有效缺口是 `x=8`，后续三人
  应在此汇合，不再沿墙向东搜索。
- 移动落点确认后、行动结束前，unit record 的当前坐标仍保留原值，画面则显示暂存位置；例如
  鸣人桥面候选的游标为 `(12,19)`、record 仍为 `(10,19)`，完成“行动结束”后才更新为
  `(12,19)`。这一中间态不是卡死，也不能提前作为已提交坐标证据。
- 正式恢复点为 `artifacts/runtime-checkpoints/scenario-46-turn23-wall-gap.ss9`，SHA-256
  `1df246eb111c76cb2c8ff1b0080e1b04a62d729d69d9aa29285e89b16ad75a18`。1200 帧零输入
  run `e51cbba61a2521d55d0a3e9239e630ea` 成功，峰值 owned-tree RSS 52.09765625 MiB，
  `pgid_clean=true`；恢复后为玩家回合。
- 第二十三回合我方坐标为鸣人 `(12,19)`、佐助 `(8,11)`、小樱 `(9,14)`，HP 仍为
  109/89/80；五只猫位于 `(7,3)/(1,3)/(5,3)/(2,3)/(4,3)`，均为 HP 1，结果仍为 0。
  后续从该恢复点让小樱、鸣人向 `x=8` 缺口汇合，佐助继续北上，在 `y<=5` 开始原地直接攻击。
- 本段所有模拟器启动均经 heavy guard 串行执行；发现桌面执行单元可能在 shell 批次尚有下一次
  输入运行时提前交还控制权后，后续改为 Codex 逐次启动、等待 audit 成功并确认 `pgrep -x mGBA`
  为空，再启动下一次。mGBA crash report 仍为 25，系统可用内存为 62%。
