# Task 6 Step 5.2 — technique-menu p24 serial acceptance

Run exactly two fresh, serial, guarded zero-input capture24 replays on observer ROM SHA `2e0ef35475737789103403cfea454ace1de0ae6b6ed1789d49d7e55d5dc1737f`: candidate `build/scenario-41-defense-yes-confirm-a-20260717/run/output.ss9` SHA `f26173ff9d03344cd2f1bb6715158e5764a1853eb8c808df811b852545a9a0f1` → p1 → p2. Use the existing zero-input runner through the shared heavy guard. Start p2 only after p1 independently passes every gate. Write only fresh `build/scenario-41-technique-menu-p24-1-20260717/`, `build/scenario-41-technique-menu-p24-2-20260717/`, and `.superpowers/sdd/task-6-technique-menu-p24-report.md`.

Require strict lineage: p1 input is the named candidate and p2 input SHA exactly equals p1 output SHA. Both runs must record `inputs=[]`, no pre-script (these are ordinary p-length replays, not instrumentation samples), capture/frame 24, completed/0 non-degraded guard, exact state/ROM/runner provenance, RSS and owned process/listener/crash/base.sav cleanup. Candidate, p1 and p2 must have exact full-screen normalized RGB SHA `17644dae174acd0e26b9b004f3e8a03b9fd9a43b523cb3dff40f978b4f9b6eef`; task2/list handler and all saved registers, menu/round, battle/map, current object, complete `0x1D4` unit, counter1, complete MOD1 and canonical-zero MOD2 must also be exact across all three.

Stop on the first mismatch. If all gates pass, only p1 may be proposed as the stable reusable technique-menu candidate; p2 is corroboration only. No key, third replay, checkpoint copy/persistence, tracked edit, task checkoff, commit or push. Both outputs remain forbidden pending parent audit and fresh independent review.
